# Backend Server URL
# For development (when running backend locally on port 5000):
# VITE_BACKEND_URL=http://localhost:5000
# For production (Render deployment):
VITE_BACKEND_URL=https://collaborative-drawing-app-backend-wz3c.onrender.com
# Note: After changing this file, create a .env file with your actual values
# cp .env.example .env
# Then restart the development server
